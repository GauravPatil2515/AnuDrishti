import React from 'react';

// Lightweight stub so the app shell compiles. The interactive assistant lives
// inside the PharmaGuard AI Workbench explanation audit for the SIH demo.
const ChemBioBot = () => null;

export default ChemBioBot;
