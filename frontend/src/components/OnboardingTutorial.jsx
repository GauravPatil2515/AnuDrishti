import React from 'react';

// Lightweight stubs so the app shell compiles. The SIH deliverable is the
// PharmaGuard AI Workbench; these global helpers are no-ops for the demo build.
export const OnboardingTutorial = () => null;
export const QuickHelp = () => null;

export default OnboardingTutorial;
